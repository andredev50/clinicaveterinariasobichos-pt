# Reference images

_No reference images from the source website passed the score threshold._

For the bundled extracted assets (`public/images/`) and per-icon SVGs (`public/images/icons/`), see `plan.md` § Assets - that's the canonical asset list.
